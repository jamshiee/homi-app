export enum PriceUnitEnum {
  TOTAL = 'total',
  PER_CENT = 'per_cent',
  PER_ACRE = 'per_acre',
  PER_SQFT = 'per_sqft',
  PER_SQM = 'per_sqm',
  PER_NIGHT = 'per_night',
  PER_MONTH = 'per_month',
}